{
	"name": "@phenomen/foundry-vtt-ru",
	"private": "true",
	"scripts": {
		"build": "vite build",
		"test": "vite build && pnpm process:test",
		"release": "bumpp public/module.json README.md --no-push --no-commit --no-tag && vite build && pnpm process:pack",
		"format:check": "prettier --check .",
		"format": "prettier --write .",
		"process:pack": "node process-pack.js",
		"process:test": "node process-test.js"
	},
	"type": "module",
	"devDependencies": {
		"archiver": "^7.0.0",
		"bumpp": "^9.4.0",
		"dotenv": "^16.4.5",
		"prettier": "^3.2.5",
		"vite": "^5.1.5"
	}
}
